Class.allInstances()->collect(oclAsType(OclAny))->union(
	Attribute.allInstances()
)->union(
	Class.allInstances()->select(not parent.oclIsUndefined())
)->union(
	Association.allInstances()->select(upperBound = -1)
)->union(
	Association.allInstances()->select(upperBound = -1)
)->union(
	Association.allInstances()->select(upperBound = 1)
)