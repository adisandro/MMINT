(armController, {Contract .
  A := (
    (atom fault_angleSensor_armController).neg &
    (atom fault_LAAPActive_armController).neg &
    (atom fault_LAAPFlow_armController).neg &
    (atom fault_operatorControlLever_armController).neg
  ).always,
  G := (atom fault_armFlow_armController).neg.always
})